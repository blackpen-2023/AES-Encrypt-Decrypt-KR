# AES-Encrypt-Decrypt-KR

한글을 포함한 문자열을 **AES-128-CBC**로 암호화/복호화하고,  
추가로 **영문/숫자 기반 문자열을 한글로 매핑해서 숨기는 도구**입니다.

- `AES_key_generator.py`  
  - 한글 문자열을 AES로 암호화/복호화하는 CLI 스크립트
- `mapping_key_generator.py`  
  - 영문/숫자 문자열 ↔ 한글 매핑을 통해 키/암호문을 한글처럼 보이게 숨기거나 다시 복원하는 스크립트

> ⚠️ 이 프로젝트는 **학습 및 개인 실험용** 예제입니다.  
> 실제 보안환경에서 사용하려면 추가적인 검토가 필요합니다.

---

## 🔧 Requirements

- Python 3.x  
- PyCryptodome

설치:

```bash
pip install pycryptodome
```

---

## 📁 파일 구성

### `AES_key_generator.py`
- AES-128-CBC 방식으로 문자열 암호화/복호화
- Base64 인코딩된 키와 암호문 입·출력
- CLI 기반

### `mapping_key_generator.py`
- 영문/숫자 ↔ 한글 매핑 변환 기능
- reverse + Caesar shift + mapping 조합
- AES 키나 암호문을 한글처럼 숨길 때 사용 가능

---

## ▶ 사용법

### 1. AES 암호화 / 복호화  
```bash
python AES_key_generator.py
```

실행 후 선택:

- `a` : 암호화  
- `d` : 복호화  
- `q` : 종료  

#### 🔐 암호화 예시

```
모드 선택 (a: AES 암호화, d: AES 복호화, q: 종료): a
암호화할 한글 문자열을 입력하세요: 나만의 메시지
암호문: <Base64 문자열>
복호화를 위한 키: <Base64 AES 키>
```

#### 🔓 복호화 예시

```
모드 선택 (a: AES 암호화, d: AES 복호화, q: 종료): d
암호문을 입력하세요: <Base64>
AES 키를 입력하세요: <Base64>
복호화 결과: 나만의 메시지
```

---

## 2. 한글 매핑 기반 문자열 변환  
```bash
python mapping_key_generator.py
```

선택:

- `e` : 암호문(영문/숫자) → 한글 변환  
- `r` : 한글 변환 문자열 → 원래 암호문 복원  

### 2-1. 모드 `e`

```
작업을 선택하세요 (e: 복호화 / r: 복호화키 해독): e
암호문을 입력하세요: <영문/숫자 문자열>
복호화 결과: <한글처럼 보이는 문자열>
```

변환 방식:
- 문자열 뒤집기
- Caesar Shift(-2)
- 매핑 테이블로 한글 치환

### 2-2. 모드 `r`

```
작업을 선택하세요 (e: 복호화 / r: 복호화키 해독): r
복호화된 한글을 입력하세요: <한글 문자열>
원래 암호문: <영문/숫자 문자열>
```

복원 과정:
- 한글 → 영문/숫자 역매핑
- Caesar Shift(+2)
- 문자열 뒤집기

---

## ⚠ 보안 관련 주의사항

- `mapping_key_generator.py` 의 변환은 **보안 암호화가 아닌 난독화** 수준입니다.
- 실제 서비스에서는 AES 키 관리, 전송 암호화(HTTPS/TLS), 안전한 키 저장 등을 반드시 고려해야 합니다.
